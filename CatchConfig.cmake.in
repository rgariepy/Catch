# - Config file for the robochunk package
# It defines the following variables
# easyloggingpp_INCLUDE_DIRS - include directories for the robochunk libraries

# Compute Paths
get_filename_component(CATCH_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(Catch_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
message(STATUS "Found Catch")
message(STATUS "   Includes: ${Catch_INCLUDE_DIRS}")
